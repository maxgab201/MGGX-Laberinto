<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:glEsVersion="0x00030000" android:required="true" />
    <uses-feature android:name="android.hardware.gamepad" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
        El juego entero anda SIN internet: la cueva, las texturas, los modelos,
        la musica y los efectos se generan por codigo, y el perfil vive en el
        telefono. Lo unico que necesita red es el multijugador, que habla con
        el relay de Firebase.

        Los dos permisos se declaran aca aunque la libreria de Firebase ya los
        traiga en su propio manifest y se mezclen solos. Un permiso que entra
        escondido por una dependencia es un permiso que nadie sabe que esta:
        no se ve leyendo el manifest del proyecto, y el dia que se cambie de
        servicio se rompe sin que nadie entienda por que. Aca queda escrito
        que los pedimos nosotros y para que.

        ACCESS_NETWORK_STATE es el que permite preguntar si hay red ANTES de
        abrir una sala (ver PuedeJugarEnRed). Sin el, Firebase offline no
        falla: guarda todo en una cola local y la sala se queda esperando para
        siempre sin decir nada.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".GameApp"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:theme="@style/Theme.MggxLaberinto">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="sensorLandscape"
            android:resizeableActivity="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|navigation|uiMode|density|smallestScreenSize"
            android:theme="@style/Theme.MggxLaberinto">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
    </application>
</manifest>
